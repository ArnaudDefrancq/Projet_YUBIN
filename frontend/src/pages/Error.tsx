import React from 'react';
import MessageError from '../components/MessageError';


const Error = () => {
    return (
        <div>
            <MessageError />
        </div>
    );
};

export default Error;